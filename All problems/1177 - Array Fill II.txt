import 'dart:io';
void main(){
  int i = 0;
  int count = 0;
    int readLine = int.parse(stdin.readLineSync());
    List<int> list = [];
    for(i; i<1000; i++){
      list.add(i);
    if(count == readLine){
      count = 0;
    }
    print("N[${list[i]}] = $count");
    count++;
  }
  
}

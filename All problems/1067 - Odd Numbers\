import 'dart:io';

void main() {
  int x = int.parse(stdin.readLineSync());

  if (x >= 0 && x <= 1000) {
    int i;
    for (i = 1; i <= x; i++) {
      if (i % 2 != 0) {
        print(i);
      }
    }
  }
}
